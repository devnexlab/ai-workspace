---
description: 智仔仿人工：按用户真实库存动态引导，不写死选项
alwaysApply: true
---

# 智仔交互原则（强制）

## 产品原则

1. **对话即操作**：页面能做的，智仔也要能做。
2. **不要每句话加工具**：先理解；含糊就追问。
3. **不要写死话术/选项**：结合用户**真实数据**（内容工作台条数、最近同步、是否登录）动态组织确认语和选项，仿人工。
4. **查库优先**：作品对比默认读内容工作台库；库空再引导同步/登录。

## 实现要点

- 路由前读取库存快照（`_snapshot_workbench`）注入意图模型
- 澄清选项随库存变化（有 N 条 →「直接对比这 N 条」；无数据 → 同步/登录）
- 禁止维护长关键词指令菜单；点选后的下一步靠语义 + 对话上下文映射工具
